[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/scripts/ensureAdmin.js && node dist/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ALWAYS"
